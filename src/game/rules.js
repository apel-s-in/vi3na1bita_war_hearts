/**
 * ПРАВИЛА ИГРЫ "ВОЙНА СЕРДЕЦ" (Классический Морской Бой 10х10)
 * 
 * 1. Игровое поле: Квадрат 10х10 клеток.
 * 2. Состав флота (10 сердец-кораблей):
 *    - 1 шт: 4-клеточный (Линкор)
 *    - 2 шт: 3-клеточных (Крейсеры)
 *    - 3 шт: 2-клеточных (Эсминцы)
 *    - 4 шт: 1-клеточных (Катера)
 * 3. Расстановка:
 *    - Корабли могут располагаться только строго горизонтально или вертикально.
 *    - Запрещено ставить корабли по диагонали, заворачивать буквой "Г" или "Зигзагом".
 *    - Между любыми двумя кораблями должна быть минимум 1 пустая клетка (по всем сторонам и диагоналям).
 *    - Корабли могут касаться краев поля.
 */

export const GAME_RULES = {
  id: 'war_hearts_classic_10x10',
  boardSize: 10,
  // Конфиг: [размер_корабля, ...]
  ships: [4, 3, 3, 2, 2, 2, 1, 1, 1, 1],
  turnMode: 'classic',
  chat: true,
  voice: true
};

export const isInsideBoard = (x, y) =>
  Number.isInteger(x) && Number.isInteger(y) && x >= 0 && y >= 0 && x < GAME_RULES.boardSize && y < GAME_RULES.boardSize;
